# Get all files and folders
    # in a given dir (only first level)
# Separate folders and files
# Remove lower levels
    # If you have "/cake", "/cake/chocolate.txt", "/cake/other_cakes", then:
        # "/cake" is the only folder, and "/cake/chocolate.txt" is the only file.
